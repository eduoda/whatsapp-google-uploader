# NPM Configuration
# For Termux and environments without native compilation support,
# use: npm install --omit=optional --omit=dev

# Don't fail on optional dependencies
optional=true

# Disable package-lock for cleaner Termux installs (optional)
# package-lock=false