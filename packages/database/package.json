{
  "name": "@whatsapp-uploader/database",
  "version": "1.0.0",
  "description": "Database abstraction layer for WhatsApp Google Uploader using better-sqlite3",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist",
    "README.md",
    "CHANGELOG.md"
  ],
  "scripts": {
    "build": "tsc",
    "build:watch": "tsc --watch",
    "clean": "rimraf dist",
    "dev": "npm run build:watch",
    "test": "jest",
    "test:watch": "jest --watch",
    "lint": "eslint src --ext .ts",
    "lint:fix": "eslint src --ext .ts --fix"
  },
  "keywords": [
    "database",
    "sqlite3",
    "better-sqlite3",
    "persistence",
    "deduplication"
  ],
  "author": "WhatsApp Google Uploader",
  "license": "MIT",
  "peerDependencies": {
    "better-sqlite3": "^9.0.0"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.8",
    "@types/node": "^20.5.0",
    "typescript": "^5.1.6",
    "rimraf": "^5.0.1"
  },
  "engines": {
    "node": ">=14.0.0"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/user/whatsapp-google-uploader.git",
    "directory": "packages/database"
  }
}