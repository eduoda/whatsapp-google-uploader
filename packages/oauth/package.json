{
  "name": "@whatsapp-uploader/oauth",
  "version": "1.0.0",
  "description": "Google OAuth2 authentication library for WhatsApp Google Uploader",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist",
    "README.md",
    "CHANGELOG.md"
  ],
  "scripts": {
    "build": "tsc",
    "build:watch": "tsc --watch",
    "clean": "rimraf dist",
    "dev": "npm run build:watch",
    "test": "jest",
    "test:watch": "jest --watch",
    "lint": "eslint src --ext .ts",
    "lint:fix": "eslint src --ext .ts --fix"
  },
  "keywords": [
    "oauth",
    "google",
    "authentication",
    "whatsapp-uploader"
  ],
  "author": "WhatsApp Google Uploader",
  "license": "MIT",
  "peerDependencies": {
    "google-auth-library": "^9.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.5.0",
    "typescript": "^5.1.6",
    "rimraf": "^5.0.1"
  },
  "engines": {
    "node": ">=14.0.0"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/user/whatsapp-google-uploader.git",
    "directory": "packages/oauth"
  }
}